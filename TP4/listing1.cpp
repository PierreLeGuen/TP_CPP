C_File fifo;
C_Pile lifo;
int i;

fifo < 0 < 1;				// empile 0 puis 1 
fifo > i;						// dépile dans i
cout << "file (" << fifo.GetSize() << ") : " << i << endl;

lifo < 0 < 1;				// empile 0 puis 1 
lifo > i;						// dépile dans i
cout << "pile (" << lifo.GetSize() << ") : " << i << endl;
